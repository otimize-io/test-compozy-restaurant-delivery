/**
 * Development environment. Same shape as the production environment; the gateway base URL is
 * configurable here for local runs (default YARP gateway on http://localhost:5000).
 */
export const environment = {
  production: false,
  apiBase: 'http://localhost:5000',
};
